import React from 'react';
import { Icon, IconProps } from '@ui-kitten/components';

export const ConfirmIcon = (props: IconProps) => (
  <Icon name='checkmark-outline' {...props} />
);
export const CancelIcon = (props: IconProps) => (
  <Icon name='close-outline' {...props} />
);
export const SettingsIcon = (props: IconProps) => (
  <Icon name='settings-outline' {...props} />
);
export const HomeIcon = (props: IconProps) => (
  <Icon name='home-outline' {...props} />
);
export const ViewIcon = (props: IconProps) => (
  <Icon name='eye-outline' {...props} />
);
export const ProjectIcon = (props: IconProps) => (
  <Icon name='cube-outline' {...props} />
);
export const ArrowIcon = (props: IconProps) => (
  <Icon name='chevron-right-outline' {...props} />
);